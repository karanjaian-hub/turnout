networks:
  turnout-network:
    driver: bridge

volumes:
  postgres-data:
  redis-data:

services:

  # Infrastructure
  postgres:
    image: postgres:15-alpine
    container_name: turnout_postgres
    restart: unless-stopped
    mem_limit: 384m
    environment:
      POSTGRES_DB: turnout_db
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./scripts/init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d turnout_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - turnout-network

  redis:
    image: redis:7-alpine
    container_name: turnout_redis
    restart: unless-stopped
    mem_limit: 128m
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - turnout-network

  zookeeper:
    image: confluentinc/cp-zookeeper:7.6.0
    container_name: turnout_zookeeper
    restart: unless-stopped
    mem_limit: 384m
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"
    networks:
      - turnout-network

  kafka:
    image: confluentinc/cp-kafka:7.6.0
    container_name: turnout_kafka
    restart: unless-stopped
    mem_limit: 512m
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
      KAFKA_HEAP_OPTS: "-Xmx256m -Xms128m"
    healthcheck:
      test: ["CMD-SHELL", "kafka-topics --bootstrap-server localhost:9092 --list"]
      interval: 15s
      timeout: 10s
      retries: 5
    networks:
      - turnout-network

  # ── Application Services ────────────────────────────────────────────────────
  api-gateway:
    build:
      context: .
      dockerfile: api-gateway/Dockerfile
      args:
        SERVICE_PATH: api-gateway
    container_name: turnout_api_gateway
    restart: unless-stopped
    mem_limit: 384m
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8080/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    environment:
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: ${JWT_SECRET}
      AUTH_SERVICE_URL: http://authservice:8081
      EVENT_SERVICE_URL: http://eventservice:8082
      GUEST_SERVICE_URL: http://guestservice:8083
      EMAIL_SERVICE_URL: http://emailservice:8084
      RSVP_SERVICE_URL: http://rsvpservice:8085
      NOTIFICATION_SERVICE_URL: http://notificationservice:8086
      NOTIFICATION_SERVICE_WS_URL: ws://notificationservice:8086
      PAYMENT_SERVICE_URL: http://paymentservice:8087
      AI_SERVICE_URL: http://aiservice:8088
      JAVA_OPTS: "-Xmx128m -Xms64m"
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - turnout-network

  authservice:
    build:
      context: .
      dockerfile: services/authservice/Dockerfile
      args:
        SERVICE_PATH: services/authservice
    container_name: turnout_authservice
    restart: unless-stopped
    mem_limit: 384m
    ports:
      - "8081:8081"
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8081/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    environment:
      SERVER_PORT: 8081
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: turnout_db
      DB_USERNAME: ${POSTGRES_USER}
      DB_PASSWORD: ${POSTGRES_PASSWORD}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: ${JWT_SECRET}
      JWT_ACCESS_TOKEN_EXPIRY_MS: 900000
      JWT_REFRESH_TOKEN_EXPIRY_MS: 604800000
      BREVO_API_KEY: ${BREVO_API_KEY}
      BREVO_SENDER_EMAIL: ${BREVO_SENDER_EMAIL}
      FRONTEND_URL: ${FRONTEND_URL}
      JAVA_OPTS: "-Xmx128m -Xms64m"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - turnout-network

  eventservice:
    build:
      context: .
      dockerfile: services/eventservice/Dockerfile
      args:
        SERVICE_PATH: services/eventservice
    container_name: turnout_eventservice
    restart: unless-stopped
    mem_limit: 384m
    ports:
      - "8082:8082"
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8082/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    environment:
      SERVER_PORT: 8082
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: turnout_db
      DB_USERNAME: ${POSTGRES_USER}
      DB_PASSWORD: ${POSTGRES_PASSWORD}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      JWT_SECRET: ${JWT_SECRET}
      JAVA_OPTS: "-Xmx128m -Xms64m"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    networks:
      - turnout-network

  guestservice:
    build:
      context: .
      dockerfile: services/guestservice/Dockerfile
      args:
        SERVICE_PATH: services/guestservice
    container_name: turnout_guestservice
    restart: unless-stopped
    mem_limit: 384m
    ports:
      - "8083:8083"
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8083/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    environment:
      SERVER_PORT: 8083
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: turnout_db
      DB_USERNAME: ${POSTGRES_USER}
      DB_PASSWORD: ${POSTGRES_PASSWORD}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      JWT_SECRET: ${JWT_SECRET}
      JAVA_OPTS: "-Xmx128m -Xms64m"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    networks:
      - turnout-network

  emailservice:
    build:
      context: .
      dockerfile: services/emailservice/Dockerfile
      args:
        SERVICE_PATH: services/emailservice
    container_name: turnout_emailservice
    mem_limit: 384m
    restart: unless-stopped
    ports:
      - "8084:8084"
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8084/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    environment:
      SERVER_PORT: 8084
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: turnout_db
      DB_USERNAME: ${POSTGRES_USER}
      DB_PASSWORD: ${POSTGRES_PASSWORD}
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      REDIS_HOST: redis
      REDIS_PORT: 6379
      BREVO_API_KEY: ${BREVO_API_KEY}
      BREVO_SENDER_EMAIL: ${BREVO_SENDER_EMAIL}
      BREVO_SENDER_NAME: ${BREVO_SENDER_NAME}
      JWT_SECRET: ${JWT_SECRET}
      JAVA_OPTS: "-Xmx128m -Xms64m"
    depends_on:
      postgres:
        condition: service_healthy
      kafka:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - turnout-network

  rsvpservice:
    build:
      context: .
      dockerfile: services/rsvpservice/Dockerfile
      args:
        SERVICE_PATH: services/rsvpservice
    container_name: turnout_rsvpservice
    restart: unless-stopped
    mem_limit: 384m
    ports:
      - "8085:8085"
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8085/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    environment:
      SERVER_PORT: 8085
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: turnout_db
      DB_USERNAME: ${POSTGRES_USER}
      DB_PASSWORD: ${POSTGRES_PASSWORD}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      JWT_SECRET: ${JWT_SECRET}
      JAVA_OPTS: "-Xmx128m -Xms64m"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    networks:
      - turnout-network

  notificationservice:
    build:
      context: .
      dockerfile: services/notificationservice/Dockerfile
      args:
        SERVICE_PATH: services/notificationservice
    container_name: turnout_notificationservice
    restart: unless-stopped
    mem_limit: 384m
    ports:
      - "8086:8086"
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8086/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    environment:
      SERVER_PORT: 8086
      REDIS_HOST: redis
      REDIS_PORT: 6379
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      JWT_SECRET: ${JWT_SECRET}
      JAVA_OPTS: "-Xmx128m -Xms64m"
    depends_on:
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    networks:
      - turnout-network

  paymentservice:
    build:
      context: .
      dockerfile: services/paymentservice/Dockerfile
      args:
        SERVICE_PATH: services/paymentservice
    container_name: turnout_paymentservice
    restart: unless-stopped
    mem_limit: 384m
    ports:
      - "8087:8087"
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8087/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    environment:
      SERVER_PORT: 8087
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: turnout_db
      DB_USERNAME: ${POSTGRES_USER}
      DB_PASSWORD: ${POSTGRES_PASSWORD}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      KAFKA_BOOTSTRAP_SERVERS: kafka:29092
      JWT_SECRET: ${JWT_SECRET}
      STRIPE_SECRET_KEY: ${STRIPE_SECRET_KEY}
      MPESA_CONSUMER_KEY: ${MPESA_CONSUMER_KEY}
      MPESA_CONSUMER_SECRET: ${MPESA_CONSUMER_SECRET}
      MPESA_PASSKEY: ${MPESA_PASSKEY}
      MPESA_SHORTCODE: ${MPESA_SHORTCODE}
      MPESA_ENV: ${MPESA_ENV}
      MPESA_CALLBACK_URL: ${MPESA_CALLBACK_URL}
      STRIPE_WEBHOOK_SECRET: ${STRIPE_WEBHOOK_SECRET}
      JAVA_OPTS: "-Xmx128m -Xms64m"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    networks:
      - turnout-network

  aiservice:
    build:
      context: .
      dockerfile: services/aiservice/Dockerfile
      args:
        SERVICE_PATH: services/aiservice
    container_name: turnout_aiservice
    restart: unless-stopped
    mem_limit: 384m
    ports:
      - "8088:8088"
    healthcheck:
      test: ["CMD-SHELL", "wget --spider -q http://localhost:8088/actuator/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
    environment:
      SERVER_PORT: 8088
      REDIS_HOST: redis
      REDIS_PORT: 6379
      JWT_SECRET: ${JWT_SECRET}
      GROQ_API_KEY: ${GROQ_API_KEY}
      JAVA_OPTS: "-Xmx128m -Xms64m"
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - turnout-network