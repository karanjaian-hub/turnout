package com.turnout.authservice;

import org.junit.jupiter.api.Test;

// Full context boot requires a live database and Redis.
// Those are covered by integration tests (Phase 12.3).
// This placeholder keeps the file valid without starting Spring.
class AuthserviceApplicationTests {

    @Test
    void contextLoads() {
        // intentionally empty — no Spring context needed here
    }
}
