server:
  port: 8080

spring:
  application:
    name: api-gateway
  threads:
    virtual:
      enabled: true
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
      - org.springframework.boot.autoconfigure.data.jpa.JpaRepositoriesAutoConfiguration
  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          "[/**]":
            allowedOrigins:
              - "http://localhost:3000"
              - "http://localhost:3001"
            allowedMethods: "*"
            allowedHeaders: "*"
            allowCredentials: true
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials
      routes:
        # /api/auth/me requires a valid JWT — must go through JwtAuthentication
        - id: auth-private
          uri: http://${AUTH_SERVICE_HOST:authservice}:8081
          predicates:
            - Path=/api/auth/me, /api/auth/logout
          filters:
            - JwtAuthentication

        # Internal service-to-service endpoints — no JWT, never exposed to end users
        - id: auth-internal
          uri: http://${AUTH_SERVICE_HOST:authservice}:8081
          predicates:
            - Path=/api/internal/**

        # All other auth endpoints are public (login, register, refresh, otp etc.)
        - id: auth-public
          uri: http://${AUTH_SERVICE_HOST:authservice}:8081
          predicates:
            - Path=/api/auth/**

        - id: rsvp-public
          uri: http://${RSVP_SERVICE_HOST:rsvpservice}:8085
          predicates:
            - Path=/api/rsvp/**
          filters:
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 10
                redis-rate-limiter.burstCapacity: 20
                key-resolver: "#{@ipKeyResolver}"

        - id: payments-webhooks-public
          uri: http://${PAYMENT_SERVICE_HOST:paymentservice}:8087
          predicates:
            - Path=/api/payments/mpesa/callback, /api/payments/stripe/webhook

        - id: websocket
          uri: ws://${NOTIFICATION_SERVICE_HOST:notificationservice}:8086
          predicates:
            - Path=/ws/**

        - id: events
          uri: http://${EVENT_SERVICE_HOST:eventservice}:8082
          predicates:
            - Path=/api/events/**
          filters:
            - JwtAuthentication

        - id: guests
          uri: http://${GUEST_SERVICE_HOST:guestservice}:8083
          predicates:
            - Path=/api/guests/**
          filters:
            - JwtAuthentication

        - id: emails
          uri: http://${EMAIL_SERVICE_HOST:emailservice}:8084
          predicates:
            - Path=/api/emails/**
          filters:
            - JwtAuthentication

        - id: auth-admin-endpoints
          uri: http://${AUTH_SERVICE_HOST:authservice}:8081
          predicates:
            - Path=/api/admin/users/**, /api/admin/create-admin
          filters:
            - JwtAuthentication

        - id: admin
          uri: http://${NOTIFICATION_SERVICE_HOST:notificationservice}:8086
          predicates:
            - Path=/api/admin/**
          filters:
            - JwtAuthentication

        - id: payments
          uri: http://${PAYMENT_SERVICE_HOST:paymentservice}:8087
          predicates:
            - Path=/api/payments/**
          filters:
            - JwtAuthentication

        - id: ai
          uri: http://${AI_SERVICE_HOST:aiservice}:8088
          predicates:
            - Path=/api/ai/**
          filters:
            - JwtAuthentication

turnout:
  jwt:
    secret: ${JWT_SECRET}

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
  endpoint:
    health:
      show-details: always

---
spring:
  config:
    activate:
      on-profile: localhost
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
      - org.springframework.boot.autoconfigure.data.jpa.JpaRepositoriesAutoConfiguration
  cloud:
    gateway:
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials
      routes:
        - id: auth-private
          uri: http://localhost:8081
          predicates:
            - Path=/api/auth/me, /api/auth/logout
          filters:
            - JwtAuthentication
        - id: auth-internal
          uri: http://localhost:8081
          predicates:
            - Path=/api/internal/**


        - id: auth-public
          uri: http://localhost:8081
          predicates:
            - Path=/api/auth/**

        - id: rsvp-public
          uri: http://localhost:8085
          predicates:
            - Path=/api/rsvp/**
          filters:
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 10
                redis-rate-limiter.burstCapacity: 20
                key-resolver: "#{@ipKeyResolver}"

        - id: payments-webhooks-public
          uri: http://localhost:8087
          predicates:
            - Path=/api/payments/mpesa/callback, /api/payments/stripe/webhook

        - id: websocket
          uri: ws://localhost:8086
          predicates:
            - Path=/ws/**

        - id: events
          uri: http://localhost:8082
          predicates:
            - Path=/api/events/**
          filters:
            - JwtAuthentication

        - id: guests
          uri: http://localhost:8083
          predicates:
            - Path=/api/guests/**
          filters:
            - JwtAuthentication

        - id: emails
          uri: http://localhost:8084
          predicates:
            - Path=/api/emails/**
          filters:
            - JwtAuthentication

        - id: auth-admin-endpoints
          uri: http://localhost:8081
          predicates:
            - Path=/api/admin/users/**, /api/admin/create-admin
          filters:
            - JwtAuthentication

        - id: admin
          uri: http://localhost:8086
          predicates:
            - Path=/api/admin/**
          filters:
            - JwtAuthentication

        - id: payments
          uri: http://localhost:8087
          predicates:
            - Path=/api/payments/**
          filters:
            - JwtAuthentication

        - id: ai
          uri: http://localhost:8088
          predicates:
            - Path=/api/ai/**
          filters:
            - JwtAuthentication
