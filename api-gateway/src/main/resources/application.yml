server:
  port: 8080

spring:
  application:
    name: api-gateway
  threads:
    virtual:
      enabled: true
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
      - org.springframework.boot.autoconfigure.data.jpa.JpaRepositoriesAutoConfiguration
  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          "[/**]":
            allowedOriginPatterns:
              - "http://localhost:3000"
              - "http://localhost:3001"
              - "http://localhost:5173"
              - "http://localhost:5174"
              - "https://turnout-frontend.vercel.app"
              - "https://turnout-frontend-*.vercel.app"
              - "https://turnout-nine.vercel.app"
              - "https://turnout-*-karanja-ia.vercel.app"
            allowedMethods: "*"
            allowedHeaders: "*"
            allowCredentials: true
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials
      routes:
        # /api/auth/me requires a valid JWT — must go through JwtAuthentication
        - id: auth-private
          uri: ${AUTH_SERVICE_URL:http://localhost:8081}
          predicates:
            - Path=/api/auth/me, /api/auth/logout
          filters:
            - JwtAuthentication

        # Internal service-to-service endpoints — no JWT, never exposed to end users
        - id: auth-internal
          uri: ${AUTH_SERVICE_URL:http://localhost:8081}
          predicates:
            - Path=/api/internal/**

        # All other auth endpoints are public (login, register, refresh, otp etc.)
        - id: auth-public
          uri: ${AUTH_SERVICE_URL:http://localhost:8081}
          predicates:
            - Path=/api/auth/**

        - id: rsvp-public
          uri: ${RSVP_SERVICE_URL:http://localhost:8085}
          predicates:
            - Path=/api/rsvp/**
          filters:
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 10
                redis-rate-limiter.burstCapacity: 20
                key-resolver: "#{@ipKeyResolver}"

        - id: payments-webhooks-public
          uri: ${PAYMENT_SERVICE_URL:http://localhost:8087}
          predicates:
            - Path=/api/payments/mpesa/callback, /api/payments/stripe/webhook

        - id: websocket
          uri: ${NOTIFICATION_SERVICE_WS_URL:ws://localhost:8086}
          predicates:
            - Path=/ws/**

        - id: events
          uri: ${EVENT_SERVICE_URL:http://localhost:8082}
          predicates:
            - Path=/api/events/**
          filters:
            - JwtAuthentication

        - id: guests
          uri: ${GUEST_SERVICE_URL:http://localhost:8083}
          predicates:
            - Path=/api/guests/**
          filters:
            - JwtAuthentication

        - id: emails
          uri: ${EMAIL_SERVICE_URL:http://localhost:8084}
          predicates:
            - Path=/api/emails/**
          filters:
            - JwtAuthentication

        - id: auth-admin-endpoints
          uri: ${AUTH_SERVICE_URL:http://localhost:8081}
          predicates:
            - Path=/api/admin/users/**, /api/admin/create-admin
          filters:
            - JwtAuthentication

        - id: admin
          uri: ${NOTIFICATION_SERVICE_URL:http://localhost:8086}
          predicates:
            - Path=/api/admin/**
          filters:
            - JwtAuthentication

        - id: payments
          uri: ${PAYMENT_SERVICE_URL:http://localhost:8087}
          predicates:
            - Path=/api/payments/**
          filters:
            - JwtAuthentication

        - id: ai
          uri: ${AI_SERVICE_URL:http://localhost:8088}
          predicates:
            - Path=/api/ai/**
          filters:
            - JwtAuthentication

turnout:
  jwt:
    secret: ${JWT_SECRET}

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics
  endpoint:
    health:
      show-details: always

---
spring:
  config:
    activate:
      on-profile: localhost
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
      - org.springframework.boot.autoconfigure.data.jpa.JpaRepositoriesAutoConfiguration
  cloud:
    gateway:
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials
      routes:
        - id: auth-private
          uri: http://localhost:8081
          predicates:
            - Path=/api/auth/me, /api/auth/logout
          filters:
            - JwtAuthentication
        - id: auth-internal
          uri: http://localhost:8081
          predicates:
            - Path=/api/internal/**

        - id: auth-public
          uri: http://localhost:8081
          predicates:
            - Path=/api/auth/**

        - id: rsvp-public
          uri: http://localhost:8085
          predicates:
            - Path=/api/rsvp/**
          filters:
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 10
                redis-rate-limiter.burstCapacity: 20
                key-resolver: "#{@ipKeyResolver}"

        - id: payments-webhooks-public
          uri: http://localhost:8087
          predicates:
            - Path=/api/payments/mpesa/callback, /api/payments/stripe/webhook

        - id: websocket
          uri: ws://localhost:8086
          predicates:
            - Path=/ws/**

        - id: events
          uri: http://localhost:8082
          predicates:
            - Path=/api/events/**
          filters:
            - JwtAuthentication

        - id: guests
          uri: http://localhost:8083
          predicates:
            - Path=/api/guests/**
          filters:
            - JwtAuthentication

        - id: emails
          uri: http://localhost:8084
          predicates:
            - Path=/api/emails/**
          filters:
            - JwtAuthentication

        - id: auth-admin-endpoints
          uri: http://localhost:8081
          predicates:
            - Path=/api/admin/users/**, /api/admin/create-admin
          filters:
            - JwtAuthentication

        - id: admin
          uri: http://localhost:8086
          predicates:
            - Path=/api/admin/**
          filters:
            - JwtAuthentication

        - id: payments
          uri: http://localhost:8087
          predicates:
            - Path=/api/payments/**
          filters:
            - JwtAuthentication

        - id: ai
          uri: http://localhost:8088
          predicates:
            - Path=/api/ai/**
          filters:
            - JwtAuthentication